# ===========================================
# CHAT APPLICATION ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and update with your actual values
# NEVER commit the .env file to version control!

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# MongoDB connection string - use MongoDB Atlas or local MongoDB
MONGODB_URI=mongodb://localhost:27017/chat-app
# Optional: Specify database name (defaults to 'chat-app')
MONGO_DB_NAME=chat-app

# ===========================================
# AUTHENTICATION & SECURITY
# ===========================================
# JWT secret for signing tokens - MUST be at least 32 characters
# Generate strong secret: https://generate-secret.vercel.app/
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters-required

# Cookie signing secret - use a strong random string
COOKIE_SECRET=your-strong-cookie-secret-here

# ===========================================
# SERVER CONFIGURATION
# ===========================================
# Environment mode (development, production, staging)
NODE_ENV=development

# Server port (default: 5001)
PORT=5001

# ===========================================
# CLOUDINARY (OPTIONAL - for image uploads)
# ===========================================
# Leave empty to disable image uploads via Cloudinary
# Get credentials from: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# ===========================================
# FRONTEND (OPTIONAL)
# ===========================================
# Only needed if deploying frontend and backend on different domains
# VITE_API_URL=https://your-backend-domain.com
